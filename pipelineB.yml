# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

# pipelineB.yml
resources:
  pipelines:
    - pipeline: DemoProject Main Pipeline
      source: "DemoProject Main Pipeline"
      tags: 
        - publish-ready     # ← tag filter

trigger: none

steps:
- script: echo "Triggered because Pipeline-A used tag publish-ready"

